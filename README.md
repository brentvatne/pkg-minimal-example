# pkg-minimal-example

Minimal example using [zeit/pkg](https://github.com/zeit/pkg) to package
node project as an executable.

## Build it yourself

```
npm i -g pkg

# cd into this repo
pkg . --out-dir output
```
